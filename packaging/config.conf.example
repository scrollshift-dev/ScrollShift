# ScrollShift default configuration
# Conventional wheel mice are discovered automatically. Touchpads and touchscreens are ignored.
mode = auto
profile = balanced
reconnect_ms = 1000

# For unusual hardware, `scrollshift configure /dev/input/eventN` writes a
# mode = device override using stable vendor/product/name identity.
